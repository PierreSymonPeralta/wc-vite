{
  "name": "wc-vite-project",
  "version": "0.0.0",
  "scripts": {
    "start": "vite",
    "build": "rm -rf dist && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.52.1",
    "vite": "^2.3.0"
  }
}
